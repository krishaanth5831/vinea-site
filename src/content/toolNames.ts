/** The tool heads the page can draw. Kept separate so content has no imports
 *  that reach into components. */
export type ToolHeadName =
  | "cutter"
  | "deleafer"
  | "scout"
  | "gripper"
  | "fork";
